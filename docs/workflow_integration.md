# Workflow Integration

MSATK is designed for non-interactive workflows.

```bash
msatk profile input.fasta --out results --force
```

Workflow-friendly outputs:

- `summary.json`
- `parameters.yaml`
- `msatk.log`
- CSV tables under `tables/`
- stable `report.html`
