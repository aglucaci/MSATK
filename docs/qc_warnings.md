# QC Warnings

MSATK writes plain-language warnings to the terminal, `qc_warnings.txt`, `summary.json`, and the HTML report.

Warnings currently cover:

- high-gap sequences
- high-gap sites
- non-rectangular alignments
- unusually divergent sequences
- codon alignments that are not divisible by three
- internal stop codons in codon mode
